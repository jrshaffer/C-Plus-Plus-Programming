/*
  File: bodes_law_template.cpp
  Created by: ??
  Creation Date: ??
  Synopsis:
	Computes the distances of planets Earth, Saturn, and Neptune from the sun in
	astronomical units, miles, and meters
*/

#include <iostream>
#include <cmath>
using namespace std;
 
int main()
{
	// const double au_to_miles(0.0);
	// const double miles_to_meters(0.0);
	
	double dist_au_earth(0.0), dist_au_saturn(0.0), dist_au_neptune(0.0);
	double dist_miles_earth(0.0), dist_miles_saturn(0.0), dist_miles_neptune(0.0);
	double dist_meters_earth(0.0), dist_meters_saturn(0.0), dist_meters_neptune(0.0);
	
	// dist_au_earth = ??;
	// dist_au_saturn = ??;
	// dist_au_neptune = ??;
	
	// dist_miles_earth = ??;
	// dist_miles_saturn = ??;
	// dist_miles_neptune = ??;

	// dist_meters_earth = ??;
	// dist_meters_saturn = ??;
	// dist_meters_neptune = ??;	
	
	cout << endl << "Planet  Astro Units (est.)  Miles (est.)" << endl ;
	cout << "----------------------------------------" << endl;
	cout << "Earth" << dist_au_earth << dist_miles_earth << endl ;
	cout << "Saturn" << dist_au_saturn << dist_miles_saturn <<  endl;
	cout << "Neptune" << dist_au_neptune << dist_miles_neptune << endl ;
	
	return 0;
}  